{
  "name": "tannerwuster.github.io",
  "private": true,
  "type": "module",
  "description": "Local: `npm run dev` (Vite, port 3000). Or `npm run serve:prod` to build then preview. Do not use a generic static server on the repo root for development.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "serve:prod": "npm run build && vite preview"
  },
  "dependencies": {
    "aos": "^2.3.4",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "sass": "^1.77.0",
    "vite": "^6.0.3"
  }
}
